document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('nav').forEach(nav=>{const b=nav.querySelector('.nav-toggle'),tabs=nav.querySelector('.tabs');if(!b||!tabs)return;b.addEventListener('click',()=>{const open=nav.classList.toggle('menu-open');b.setAttribute('aria-expanded',String(open));b.textContent=open?'✕':'☰'});tabs.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('menu-open');b.textContent='☰'}))})});
