document.addEventListener('DOMContentLoaded',()=>{if(!window.AGRIVISION_IMPORTED)return;const m=MISSIONS_DATA.derniere_mission,r=MISSIONS_DATA.repartition,k=document.querySelectorAll('.kpi');document.querySelector('.hero-final .eyebrow').textContent=`MISSION IMPORTÉE · ${m.date}`;document.querySelector('.hero-metric strong').textContent=m.sante_globale+'%';const vals=[[m.hectares_analyses+' ha','Surface classée'],[ZONES_GEOJSON.features.length,'Zones QGIS'],[m.zones_critiques,'Zones prioritaires'],['QGIS','Source importée']];k.forEach((e,i)=>{if(!vals[i])return;e.querySelector('strong').textContent=vals[i][0];e.querySelector('span').textContent=vals[i][1]});const spans=document.querySelectorAll('.progress-stack span');[r.saine,r.stress_leger,r.stress_severe].forEach((v,i)=>{if(spans[i])spans[i].style.width=v+'%'});const p=document.querySelector('.progress-stack')?.nextElementSibling;if(p)p.innerHTML=`<b>${r.saine}%</b> de vigueur élevée · <b>${r.stress_leger}%</b> à surveiller · <b>${r.stress_severe}%</b> en priorité forte.`;const truth=document.querySelector('.truth-banner div');if(truth)truth.innerHTML=`<strong>Données QGIS importées.</strong> Mission « ${m.parcelle} », ${m.hectares_analyses} ha et ${ZONES_GEOJSON.features.length} zones. Les pages Carte, Historique, Rapport et Assistant utilisent maintenant cette mission.`});